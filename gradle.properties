# Pinned to what the live server actually runs - see ../../pack/versions.env and
# ../../server/mods/modrinth.txt (fabric-api entry). Don't drift these independently;
# if the server's versions change, update here too so local dev matches production.
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.19.5

# Loom itself and the Fabric API artifact version - confirmed against FabricMC's own
# fabric-example-mod repo (1.21.1 branch) rather than guessed.
loom_version=1.17-SNAPSHOT
fabric_api_version=0.116.17+1.21.1
penchant_version=0.3.7+mc1.21.1

mod_version=0.3.0
maven_group=dev.camerrron
archives_base_name=maxenchant

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
